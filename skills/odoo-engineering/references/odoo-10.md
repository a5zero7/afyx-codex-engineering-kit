# Odoo 10

Treat this as a legacy target. Verify the repository's Python and ORM conventions before editing; preserve existing legacy patterns unless the task explicitly requires a supported migration. Do not introduce later-release APIs based only on familiarity.
