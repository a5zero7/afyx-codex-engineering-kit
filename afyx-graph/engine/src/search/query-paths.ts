/** Stable import path for path references in queries; implemented in ./path-references. */
export { queryMightContainPaths, extractQueryPaths } from './path-references';
export type { QueryPathExtraction } from './path-references';
